Int x; // This is an integer
Float pi;
String yStr;

// This is a line starting with a comment
//

x = 11;
x += x + 1;
pi = 3.14;
print ("x=", x);
print ("pi=", pi);

if x in y or x notin z

Bool b = T;
Bool a = F;

yStr = 'Hello';
print ("yStr=", yStr);

yStr = "Can't";
print ("yStr=", yStr);

x = 10*3 + 7;
if x > 20:
    print ("x=", x);
    yStr = "yes";
else:
   print ("x is low");
endif;

yStr = 'Can\'t wait';
print (yStr);

